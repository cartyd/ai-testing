{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for frontend and backend",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}